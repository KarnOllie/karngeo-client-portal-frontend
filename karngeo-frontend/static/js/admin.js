document.addEventListener('DOMContentLoaded', () => {
  // Example: fetch users and files via API and populate UI
  console.log('Admin dashboard loaded');
  // Implement AJAX calls here to fetch/manage users and files
});